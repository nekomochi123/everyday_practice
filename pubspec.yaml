name: everyday_practice
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks:
  freezed_annotation:
  go_router:
  gap:
  hooks_riverpod:
  cupertino_icons:
  flutter_localizations:
    sdk: flutter
  http:

dev_dependencies:
  build_runner:
  flutter_lints:
  flutter_test:
    sdk: flutter
  freezed:
  json_serializable:

flutter:
  uses-material-design: true
